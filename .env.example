DATA_MODE=mock
ADMIN_EMAIL=admin@keremteknik.local
ADMIN_PASSWORD=changeme
JWT_SECRET=dev-jwt-secret-change-in-production
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Docker (docker compose --env-file docker/.env up -d --build)
# APP_PORT=3060
# NEXT_PUBLIC_SITE_URL=https://alanadi.com

# Production için ileride kullanılacak:
DATABASE_URL=
REDIS_URL=
